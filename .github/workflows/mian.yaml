on:
  push:
    branches:
      - main

jobs:
  e2e-tests:
    runs-on:  windows-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1

      - name: Install dependencies and run tests
        run: npm i -D @playwright/test && npx playwright install && npm test


